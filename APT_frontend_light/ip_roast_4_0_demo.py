#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
IP ROAST 4.0 - Интеллектуальная система тестирования на проникновение
Демо версия с сигнатурным анализом и 25 модулями

Особенности версии 4.0:
- Сигнатурный анализ сервисов
- Интеллектуальный выбор атак
- Адаптивное сканирование
- 25 специализированных модулей
- Конструктор сценариев атак
"""

import asyncio
import json
import logging
import time
from datetime import datetime
from typing import Dict, List, Optional, Any
import sqlite3
from pathlib import Path

# Настройка русского логирования
logging.basicConfig(
    level=logging.INFO,
    format='%(asctime)s - %(levelname)s - %(message)s',
    handlers=[
        logging.FileHandler('ip_roast_4_0.log', encoding='utf-8'),
        logging.StreamHandler()
    ]
)
logger = logging.getLogger('IP_ROAST_4.0')

class ИнтеллектуальныйМодуль:
    """Базовый класс для всех интеллектуальных модулей"""

    def __init__(self, имя: str, приоритет: int = 5):
        self.имя = имя
        self.приоритет = приоритет
        self.статус = "готов"
        self.результаты = {}

    async def выполнить(self, данные: Dict) -> Dict:
        """Выполнение модуля"""
        logger.info(f"🔧 Выполняется модуль: {self.имя}")
        await asyncio.sleep(0.1)  # имитация работы
        return {"статус": "выполнено", "модуль": self.имя}

class СканерСигнатур(ИнтеллектуальныйМодуль):
    """Модуль сканирования и определения сигнатур сервисов"""

    def __init__(self):
        super().__init__("Сканер сигнатур", 10)
        self.база_сигнатур = self._инициализировать_базу()

    def _инициализировать_базу(self):
        """Инициализация базы сигнатур"""
        return {
            80: {"сервис": "HTTP", "сигнатуры": ["Server: Apache", "Server: nginx"]},
            443: {"сервис": "HTTPS", "сигнатуры": ["SSL/TLS"]},
            22: {"сервис": "SSH", "сигнатуры": ["SSH-2.0-OpenSSH"]},
            21: {"сервис": "FTP", "сигнатуры": ["220 FTP"]},
            3306: {"сервис": "MySQL", "сигнатуры": ["mysql_native_password"]},
            5432: {"сервис": "PostgreSQL", "сигнатуры": ["PostgreSQL"]},
        }

    async def выполнить(self, данные: Dict) -> Dict:
        """Сканирование портов и определение сигнатур"""
        цель = данные.get("цель", "127.0.0.1")
        порты = данные.get("порты", [21, 22, 80, 443, 3306, 5432])

        logger.info(f"🔍 Сканируем {цель}, портов: {len(порты)}")

        найденные_сервисы = {}
        открытые_порты = []

        # Имитация сканирования портов
        for порт in порты:
            if порт in self.база_сигнатур:
                # Имитируем что порт открыт с вероятностью 30%
                import random
                if random.random() < 0.3:
                    открытые_порты.append(порт)
                    найденные_сервисы[порт] = self.база_сигнатур[порт]["сервис"]
                    logger.info(f"  ✅ Порт {порт}: {self.база_сигнатур[порт]['сервис']}")

        return {
            "цель": цель,
            "открытые_порты": открытые_порты,
            "сервисы": найденные_сервисы,
            "количество_найденных": len(открытые_порты),
            "статус": "завершено"
        }

class ИнтеллектуальныйАнализатор(ИнтеллектуальныйМодуль):
    """Интеллектуальный модуль для анализа и выбора атак"""

    def __init__(self):
        super().__init__("Интеллектуальный анализатор", 9)
        self.модули_атак = self._загрузить_модули_атак()

    def _загрузить_модули_атак(self):
        """Загрузка информации о доступных модулях атак"""
        return {
            "web_attack": {
                "применимо_к": ["HTTP", "HTTPS"],
                "атаки": ["SQL Injection", "XSS", "CSRF"],
                "эффективность": 8
            },
            "ssh_attack": {
                "применимо_к": ["SSH"],
                "атаки": ["Brute Force", "Key Scanning"],
                "эффективность": 6
            },
            "db_attack": {
                "применимо_к": ["MySQL", "PostgreSQL"],
                "атаки": ["SQL Injection", "Privilege Escalation"],
                "эффективность": 9
            },
            "ftp_attack": {
                "применимо_к": ["FTP"],
                "атаки": ["Anonymous Login", "Buffer Overflow"],
                "эффективность": 5
            }
        }

    async def выполнить(self, данные: Dict) -> Dict:
        """Анализ найденных сервисов и выбор атак"""
        сервисы = данные.get("сервисы", {})

        logger.info("🧠 Анализируем цель и выбираем оптимальные атаки...")

        рекомендованные_атаки = []
        план_атаки = {}

        for порт, сервис in сервисы.items():
            for модуль_id, модуль_инфо in self.модули_атак.items():
                if сервис in модуль_инфо["применимо_к"]:
                    рекомендованные_атаки.append({
                        "модуль": модуль_id,
                        "цель_порт": порт,
                        "цель_сервис": сервис,
                        "атаки": модуль_инфо["атаки"],
                        "эффективность": модуль_инфо["эффективность"]
                    })
                    logger.info(f"  🎯 {модуль_id} -> {сервис} (порт {порт})")

        # Сортировка по эффективности
        рекомендованные_атаки.sort(key=lambda x: x["эффективность"], reverse=True)

        return {
            "рекомендованные_атаки": рекомендованные_атаки,
            "общий_рейтинг_риска": len(рекомендованные_атаки) * 1.5,
            "статус": "анализ_завершен"
        }

class МодульВебАтак(ИнтеллектуальныйМодуль):
    """Специализированный модуль для атак на веб-приложения"""

    def __init__(self):
        super().__init__("Модуль веб-атак", 8)

    async def выполнить(self, данные: Dict) -> Dict:
        """Выполнение веб-атак"""
        logger.info("🌐 Запускаем модуль веб-атак...")

        результаты_атак = {
            "sql_injection": {"найдено": 2, "критичность": "высокая"},
            "xss": {"найдено": 1, "критичность": "средняя"},
            "csrf": {"найдено": 0, "критичность": "низкая"},
            "directory_traversal": {"найдено": 1, "критичность": "средняя"}
        }

        for атака, результат in результаты_атак.items():
            if результат["найдено"] > 0:
                logger.warning(f"  ⚠️  {атака}: найдено {результат['найдено']} уязвимостей")

        return {"результаты_атак": результаты_атак, "статус": "атаки_выполнены"}

class ГенераторОтчетов(ИнтеллектуальныйМодуль):
    """Модуль для создания детальных отчетов"""

    def __init__(self):
        super().__init__("Генератор отчетов", 1)

    async def выполнить(self, данные: Dict) -> Dict:
        """Создание отчета"""
        logger.info("📋 Создаем отчет о тестировании...")

        timestamp = datetime.now().strftime("%Y%m%d_%H%M%S")
        имя_файла = f"ip_roast_report_{timestamp}.html"

        отчет_html = self._создать_html_отчет(данные)

        with open(имя_файла, 'w', encoding='utf-8') as f:
            f.write(отчет_html)

        logger.info(f"📄 Отчет создан: {имя_файла}")

        return {"файл_отчета": имя_файла, "статус": "отчет_создан"}

    def _создать_html_отчет(self, данные: Dict) -> str:
        """Создание HTML отчета"""
        return f"""
<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <title>IP ROAST 4.0 - Отчет о тестировании</title>
    <style>
        body {{ font-family: Arial, sans-serif; margin: 20px; }}
        .header {{ background: #2c3e50; color: white; padding: 20px; border-radius: 5px; }}
        .section {{ margin: 20px 0; padding: 15px; border-left: 4px solid #3498db; }}
        .vulnerability {{ background: #ffebee; padding: 10px; margin: 5px 0; }}
        .success {{ color: #27ae60; }}
        .warning {{ color: #f39c12; }}
        .error {{ color: #e74c3c; }}
    </style>
</head>
<body>
    <div class="header">
        <h1>🚀 IP ROAST 4.0 - Отчет о тестировании</h1>
        <p>Дата: {datetime.now().strftime('%Y-%m-%d %H:%M:%S')}</p>
    </div>

    <div class="section">
        <h2>📊 Результаты сканирования</h2>
        <p>Цель: {данные.get('цель', 'Не указана')}</p>
        <p>Найдено открытых портов: {len(данные.get('открытые_порты', []))}</p>
        <p>Обнаружено сервисов: {len(данные.get('сервисы', {}))}</p>
    </div>

    <div class="section">
        <h2>🛡️ О системе</h2>
        <p>Отчет создан системой IP ROAST 4.0 - интеллектуальной платформой 
        для тестирования на проникновение с сигнатурным анализом.</p>
    </div>
</body>
</html>
        """

class КонструкторМодулей:
    """Конструктор для создания и управления модулями атак"""

    def __init__(self):
        self.доступные_модули = {
            "1_signature_scanner": СканерСигнатур,
            "4_decision_engine": ИнтеллектуальныйАнализатор,
            "7_web_attack_module": МодульВебАтак,
            "21_report_generator": ГенераторОтчетов,
        }
        self.активные_модули = {}
        logger.info("🏗️  Конструктор модулей инициализирован")

    def создать_модуль(self, тип_модуля: str):
        """Создание экземпляра модуля"""
        if тип_модуля in self.доступные_модули:
            модуль = self.доступные_модули[тип_модуля]()
            self.активные_модули[тип_модуля] = модуль
            logger.info(f"✅ Создан модуль: {модуль.имя}")
            return модуль
        else:
            logger.error(f"❌ Неизвестный тип модуля: {тип_модуля}")
            return None

    def получить_модуль(self, тип_модуля: str):
        """Получение существующего модуля"""
        return self.активные_модули.get(тип_модуля)

class ГлавнаяСистема:
    """Главная система IP ROAST 4.0"""

    def __init__(self):
        self.версия = "4.0.0"
        self.конструктор = КонструкторМодулей()
        self.история_атак = []

        logger.info(f"🚀 IP ROAST {self.версия} запущен!")
        self._показать_баннер()

    def _показать_баннер(self):
        """Показ приветственного баннера"""
        баннер = f"""
╔══════════════════════════════════════════════════════════════╗
║                     🚀 IP ROAST 4.0 🚀                      ║
║              Интеллектуальная система пентестинга             ║
║                                                              ║
║  🔹 Сигнатурный анализ сервисов                             ║
║  🔹 25 специализированных модулей                           ║
║  🔹 Интеллектуальный выбор атак                             ║
║  🔹 Адаптивное сканирование                                 ║
║  🔹 Конструктор сценариев                                   ║
║                                                              ║
║  Версия: {self.версия} | Модулей доступно: 25                     ║
╚══════════════════════════════════════════════════════════════╝
        """
        print(баннер)

    async def запустить_полное_тестирование(self, цель: str, порты: List[int] = None):
        """Запуск полного цикла тестирования"""
        logger.info(f"🎯 Начинаем полное тестирование цели: {цель}")

        # Этап 1: Сигнатурное сканирование
        сканер = self.конструктор.создать_модуль("1_signature_scanner")
        результат_сканирования = await сканер.выполнить({
            "цель": цель,
            "порты": порты or [21, 22, 80, 443, 3306, 5432, 8080]
        })

        if not результат_сканирования.get("открытые_порты"):
            logger.warning("⚠️  Открытые порты не найдены")
            return None

        # Этап 2: Интеллектуальный анализ
        анализатор = self.конструктор.создать_модуль("4_decision_engine")
        план_атаки = await анализатор.выполнить(результат_сканирования)

        # Этап 3: Выполнение атак
        результаты_атак = {}
        for атака in план_атаки.get("рекомендованные_атаки", []):
            if атака["модуль"] == "web_attack":
                модуль_атаки = self.конструктор.создать_модуль("7_web_attack_module")
                результат = await модуль_атаки.выполнить(атака)
                результаты_атак[атака["модуль"]] = результат

        # Этап 4: Создание отчета
        генератор = self.конструктор.создать_модуль("21_report_generator")

        итоговые_данные = {
            **результат_сканирования,
            "план_атаки": план_атаки,
            "результаты_атак": результаты_атак
        }

        отчет = await генератор.выполнить(итоговые_данные)

        # Сохранение в истории
        запись_истории = {
            "цель": цель,
            "время": datetime.now().isoformat(),
            "результат": итоговые_данные
        }
        self.история_атак.append(запись_истории)

        logger.info("✅ Полное тестирование завершено успешно!")
        return отчет

    def показать_доступные_модули(self):
        """Показ списка доступных модулей"""
        print("\n🧩 Доступные модули IP ROAST 4.0:")
        print("=" * 50)

        модули_инфо = [
            ("1_signature_scanner", "Сканер сигнатур", "Сигнатурный анализ"),
            ("2_service_detector", "Детектор сервисов", "Идентификация сервисов"),
            ("3_vulnerability_matcher", "Сопоставитель уязвимостей", "Поиск CVE"),
            ("4_decision_engine", "Движок принятия решений", "ИИ анализ"),
            ("5_adaptive_selector", "Адаптивный селектор", "Выбор стратегии"),
            ("7_web_attack_module", "Модуль веб-атак", "HTTP/HTTPS атаки"),
            ("8_network_attack_module", "Модуль сетевых атак", "Сетевые протоколы"),
            ("11_reconnaissance_module", "Модуль разведки", "OSINT сбор"),
            ("21_report_generator", "Генератор отчетов", "Создание отчетов"),
            ("25_configuration_manager", "Менеджер конфигураций", "Управление настройками")
        ]

        for i, (модуль_id, название, описание) in enumerate(модули_инфо, 1):
            print(f"{i:2d}. {название}")
            print(f"    ID: {модуль_id}")
            print(f"    Описание: {описание}")
            print()

        print(f"Всего модулей: 25 (показано 10 основных)")

# Демо функция
async def демо_тестирование():
    """Демонстрация возможностей системы"""
    система = ГлавнаяСистема()

    # Показ доступных модулей
    система.показать_доступные_модули()

    # Тестирование демо целей
    демо_цели = ["127.0.0.1", "192.168.1.1"]

    for цель in демо_цели:
        print(f"\n{'='*60}")
        print(f"🎯 ДЕМО ТЕСТИРОВАНИЕ: {цель}")
        print('='*60)

        try:
            результат = await система.запустить_полное_тестирование(цель)
            if результат:
                print(f"✅ Создан отчет: {результат.get('файл_отчета', 'неизвестно')}")
        except Exception as e:
            logger.error(f"❌ Ошибка при тестировании {цель}: {e}")

        print()

# Главная функция
async def main():
    """Главная функция"""
    print("🚀 Запуск IP ROAST 4.0 - Демо версия")
    await демо_тестирование()
    print("\n👋 Демонстрация завершена!")

if __name__ == "__main__":
    try:
        asyncio.run(main())
    except KeyboardInterrupt:
        print("\n⏹️  Работа прервана пользователем")
    except Exception as e:
        print(f"💥 Критическая ошибка: {e}")
